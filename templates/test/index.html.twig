

{% block body %}


{# 
    {% for basketItem in basketData %}
    <div>
        <h2>Basket Item ID: {{ basketItem.id }}</h2>
        <p>Date Added: {{ basketItem.dateAjout|date('Y-m-d H:i:s') }}</p>
        <p>Article ID: {{ basketItem.idArticle.refA }}</p>
        <p>Client ID: {{ basketItem.idClient.idU }}</p>
    </div>
{% endfor %}
 #}

<table id="Classroom" class="table table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>qt</th>
          <th>user</th>
          <th>id_produit</th>

        </tr>
      </thead>
      <h1>{{ totalPrice }}</h1>
      <tbody>
         {% for item in panier %}
          <tr>
            <td>{{  item.getIdPanier }} </td>
             <td>{{ item.getQuantiteProduct}} </td>
             <td>{{ item.idUser.getIdUser}} </td>
             <td>{{ item.IdProduit.getIdProduit}} </td>
             
             
            </tr>
            {% endfor %}
            </tbody>
        </table>
{% endblock %}