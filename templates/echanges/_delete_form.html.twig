<form method="post" action="{{ path('app_echanges_back_delete', {'idEchange': echange.idEchange}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ echange.idEchange) }}">
    <button class="btn">Delete</button>
</form>
